# افزونه ارائه‌دهنده سفارشی (CustomProvider)

این افزونه قدرتمند به شما اجازه می‌دهد پیک را به **هر سرویس‌دهنده‌ای** که با فرمت استاندارد OpenAI API سازگار است، متصل کنید. این شامل مدل‌های محلی (Local LLMs) و سایر سرویس‌های ابری می‌شود.

## 💡 چیست و چه کاربردی دارد؟

بسیاری از ابزارهای اجرای مدل‌های هوش مصنوعی، یک سرور محلی ایجاد می‌کنند که دقیقاً شبیه به OpenAI رفتار می‌کند. با این افزونه، شما می‌توانید بدون نیاز به اینترنت و به صورت کاملاً خصوصی با مدل‌های روی کامپیوتر خود چت کنید.

### ابزارهای سازگار:
-   **Ollama**: محبوب‌ترین ابزار اجرای مدل‌های Llama 3, Mistral, Gemma.
-   **LM Studio**: رابط گرافیکی برای اجرای مدل‌ها.
-   **LocalAI**: ابزار جایگزین OpenAI.
-   **Groq**: سرویس ابری فوق سریع.

## 📦 نصب

```javascript
import CustomProvider from './plugins/core/customProvider/index.js';

await peik.use(new CustomProvider());
```

## ⚙️ پیکربندی

شما می‌توانید چندین مدل سفارشی تعریف کنید. هر کدام نیاز به یک `id` یکتا دارند.

```javascript
providers: {
    custom: [
        {
            id: 'my-local-ollama',
            name: 'Ollama Local',
            endpointUrl: 'http://localhost:11434/v1/chat/completions', // آدرس استاندارد Ollama
            modelName: 'llama3', // نام مدلی که در Ollama دانلود کرده‌اید
            apiKey: 'not-needed' // برای Ollama معمولاً نیاز نیست
        }
    ]
}
```

## 🧪 تست اتصال (مثال با Ollama)

1.  [Ollama](https://ollama.com/) را نصب کنید.
2.  در ترمینال دستور `ollama run llama3` را اجرا کنید تا مدل دانلود و آماده شود.
3.  در تنظیمات پیک:
    *   آدرس: `http://localhost:11434/v1/chat/completions`
    *   مدل: `llama3`
4.  از چت با سرعت بالا و بدون نیاز به اینترنت لذت ببرید!

## 🌟 ویژگی‌ها

*   **انعطاف‌پذیری**: اتصال به صدها سرویس مختلف.
*   **حریم خصوصی**: ایده‌آل برای استفاده با مدل‌های آفلاین که داده‌ای به خارج نمی‌فرستند.
*   **بدون هزینه**: استفاده از مدل‌های متن‌باز روی سخت‌افزار خودتان.
