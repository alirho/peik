# افزونه ارائه‌دهنده OpenAI (OpenAIProvider)

این افزونه امکان اتصال هسته پیک به مدل‌های مشهور **GPT** (مانند ChatGPT) شرکت OpenAI را فراهم می‌کند.

## 🤖 درباره OpenAI

OpenAI خالق مدل‌های GPT-3.5 و GPT-4 است که استانداردهای صنعتی برای مدل‌های زبانی بزرگ محسوب می‌شوند.

## 🔑 دریافت کلید API

1.  به [OpenAI Platform](https://platform.openai.com/) بروید.
2.  ثبت‌نام کنید و در بخش API Keys یک کلید جدید بسازید.
3.  نکته: برای استفاده از API معمولاً نیاز به شارژ حساب (Credit) دارید.

## 📦 نصب

```javascript
import OpenAIProvider from './plugins/core/openaiProvider/index.js';

// پیش‌نیاز: نصب افزونه Network
await peik.use(new OpenAIProvider());
```

## ⚙️ پیکربندی

```javascript
providers: {
    openai: {
        apiKey: 'sk-proj-...', // کلید شروع شده با sk
        modelName: 'gpt-4-turbo' // یا gpt-3.5-turbo, gpt-4o
    }
}
```

## 🌟 قابلیت‌ها

*   **هوش بسیار بالا**: به خصوص در مدل‌های GPT-4.
*   **پایداری**: API بسیار پایدار و استاندارد.
*   **استریم**: پشتیبانی کامل از پاسخ‌های زنده.

## 💰 هزینه‌ها

برخلاف پلن رایگان Gemini، استفاده از APIهای OpenAI معمولاً هزینه دارد (Pay-as-you-go) و بر اساس تعداد توکن‌های مصرفی محاسبه می‌شود.

## 💻 مثال استفاده

```javascript
await peik.updateSettings({
    activeProviderId: 'openai',
    // ...
});

await chat.sendMessage('با زبان پایتون یک الگوریتم مرتب‌سازی بنویس');
```