# افزونه ذخیره‌سازی در حافظه (MemoryStorage)

این افزونه ساده‌ترین پیاده‌سازی رابط `StorageInterface` برای هسته پیک است. این افزونه تمام داده‌ها (شامل تنظیمات و تاریخچه چت‌ها) را در متغیرهای جاوااسکریپت (RAM) نگهداری می‌کند.

## 💡 چه کاری انجام می‌دهد؟

این افزونه داده‌ها را در یک `Map` ساده در حافظه نگهداری می‌کند. هیچ دیتابیس یا فایل سیستم واقعی درگیر نیست.

## 🎯 موارد استفاده

-   **توسعه و تست (Development & Testing)**: زمانی که می‌خواهید سریعاً چیزی را تست کنید و نمی‌خواهید دیتابیس مرورگر را شلوغ کنید.
-   **حالت ناشناس (Incognito)**: زمانی که کاربر می‌خواهد هیچ اثری از چت‌ها پس از بستن تب باقی نماند.
-   **دمو (Demo)**: برای نمایش قابلیت‌های برنامه بدون نیاز به تنظیمات ذخیره‌سازی.

## 📦 نصب

```javascript
import MemoryStorage from './plugins/platform/memoryStorage/index.js';

// در زمان راه‌اندازی پیک
await peik.use(new MemoryStorage());
```

## ⚙️ پیکربندی

این افزونه هیچ تنظیمات خاصی نیاز ندارد.

## ⚠️ محدودیت‌ها

*   **داده‌ها موقتی هستند**: به محض اینکه کاربر صفحه را رفرش کند یا مرورگر را ببندد، **تمام داده‌ها پاک می‌شوند**.
*   **مصرف حافظه**: برای تاریخچه‌های بسیار طولانی مناسب نیست زیرا همه چیز در RAM بارگذاری می‌شود.

## 💻 مثال استفاده

```javascript
const peik = new Peik();
await peik.use(new MemoryStorage()); // ثبت افزونه
await peik.init();

// حالا هر چتی که بسازید فقط تا زمان رفرش صفحه باقی می‌ماند
await peik.createChat('تست موقت');
```
