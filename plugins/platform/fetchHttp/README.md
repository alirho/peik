# افزونه ارتباط شبکه (FetchHttp)

این افزونه پیاده‌سازی رابط `HttpClientInterface` با استفاده از استاندارد مدرن **Fetch API** است. این لایه ارتباطی بین هسته پیک و سرورهای هوش مصنوعی (مثل گوگل و OpenAI) را برقرار می‌کند.

## 💡 چه کاری انجام می‌دهد؟

-   ارسال درخواست‌های HTTP (GET, POST, ...).
-   مدیریت هدرها و بدنه‌ی درخواست.
-   **مدیریت استریم (Streaming)**: قابلیت خواندن پاسخ‌های SSE (Server-Sent Events) برای دریافت پاسخ‌های هوش مصنوعی به صورت تایپ‌شونده.

## 📦 نصب

```javascript
import FetchHttp from './plugins/platform/fetchHttp/index.js';

await peik.use(new FetchHttp());
```

## 🌊 پشتیبانی از Stream

این افزونه از `ReadableStream` استفاده می‌کند تا داده‌ها را به محض رسیدن از شبکه (Chunk by Chunk) پردازش کند. این ویژگی برای تجربه کاربری روان در چت‌بات‌ها حیاتی است.

## ⚠️ محدودیت‌ها

*   **محیط اجرا**: این افزونه فقط در محیط‌هایی کار می‌کند که `fetch` و `ReadableStream` را پشتیبانی کنند.
    *   مرورگرهای مدرن (Chrome, Firefox, Safari, Edge).
    *   Node.js نسخه 18 و بالاتر.
    *   Deno و Bun.
*   در محیط‌های قدیمی‌تر Node.js نیاز به Polyfill دارید.

## 💻 مثال استفاده

این افزونه معمولاً به صورت مستقیم توسط شما صدا زده نمی‌شود، بلکه توسط `Provider`ها استفاده می‌شود. اما نحوه کارکرد داخلی آن به شکل زیر است:

```javascript
// استفاده داخلی در یک Provider
await this.httpClient.streamRequest(
    'https://api.example.com/chat',
    { method: 'POST', body: ... },
    (chunk) => {
        console.log('تکه جدید دریافت شد:', chunk);
    }
);
```
