# پیک (Peik)

![پیک](https://img.shields.io/badge/Peik-v0.8.0-blue)
![معماری](https://img.shields.io/badge/Architecture-Plugin--Based-orange)
![زبان](https://img.shields.io/badge/Language-Persian-green)
![مجوز](https://img.shields.io/badge/License-MIT-yellow)

**پیک** یک دستیار هوش مصنوعی پیشرفته، ماژولار و کاملاً سمت کاربر (Client-Side) است. این پروژه بر پایه یک **هسته مستقل از محیط (Environment-Agnostic Core)** بنا شده و تمام قابلیت‌های آن از جمله رابط کاربری، ذخیره‌سازی و ارتباط با مدل‌های زبانی از طریق **افزونه‌ها (Plugins)** مدیریت می‌شوند.

<center><img src="assets/images/logo.svg" alt="logo" width="80" height="80"></center>

این معماری مدرن به پیک اجازه می‌دهد تا نه تنها در مرورگر، بلکه در محیط‌های دیگر (مانند Node.js، افزونه‌های VS Code یا دسکتاپ) نیز با تغییر افزونه‌های زیرساختی قابل اجرا باشد.

## ✨ ویژگی‌های کلیدی

- **معماری افزونه‌محور (Plugin-Based)**: هسته برنامه کاملاً مستقل است و قابلیت‌ها به صورت ماژولار اضافه می‌شوند.
- **پشتیبانی از چندین ارائه‌دهنده**:
  - **Google Gemini**: استفاده از آخرین مدل‌های جمنای.
  - **OpenAI**: پشتیبانی از ChatGPT (GPT-3.5, GPT-4).
  - **مدل‌های سفارشی (Custom)**: امکان اتصال به هر API سازگار با OpenAI (مانند **Ollama** برای اجرای مدل‌های محلی).
- **رابط کاربری وب (WebUI)**: یک افزونه رابط کاربری مدرن، واکنش‌گرا و زیبا با پشتیبانی از:
  - تم تاریک و روشن.
  - مدیریت پیشرفته گپ‌ها (ویرایش نام، حذف، جستجو).
  - نمایش کدهای برنامه با قابلیت Syntax Highlighting.
  - رندرینگ کامل Markdown.
- **مدیریت هوشمند گپ‌ها**:
  - انتخاب مدل مجزا برای هر گفتگو.
  - ذخیره‌سازی خودکار و پایدار با استفاده از **IndexedDB**.
- **پشتیبانی چندرسانه‌ای**: ارسال و مشاهده تصاویر در گفتگو (با پشتیبانی از لایت‌باکس).
- **حریم خصوصی کامل**: تمام داده‌ها و کلیدهای API فقط در مرورگر شما ذخیره می‌شوند و هیچ سرور واسطی وجود ندارد.

## 🛠 معماری فنی

پروژه پیک به سه بخش اصلی تقسیم شده است:

1.  **هسته (Core)**: (`@peik/core`)
    -   مدیریت چرخه حیات برنامه.
    -   مدیریت افزونه‌ها (Plugin Manager).
    -   مدیریت وضعیت گفتگوها (Chat State).
    -   سیستم رویداد (Event Emitter).
    -   بدون هیچ وابستگی به DOM یا محیط مرورگر.

2.  **افزونه‌های پلتفرم (Platform Plugins)**:
    -   **IndexedDB Storage**: برای ذخیره‌سازی پایدار در مرورگر.
    -   **Fetch HTTP**: برای ارتباطات شبکه با استفاده از استاندارد Fetch API.

3.  **افزونه‌های ارائه (Presentation Plugins)**:
    -   **WebUI**: رابط کاربری کامل وب که با هسته تعامل می‌کند.

## 🚀 راه‌اندازی و اجرا

این پروژه به هیچ ابزار Build پیچیده‌ای نیاز ندارد و مستقیماً با استفاده از ماژول‌های ES6 اجرا می‌شود.

1.  **مخزن را کلون کنید:**
    ```bash
    git clone https://github.com/your-username/peik-ai-chat.git
    cd peik-ai-chat
    ```

2.  **اجرای سرور محلی:**
    به دلیل استفاده از ماژول‌ها، فایل `index.html` باید از طریق یک سرور وب اجرا شود:

    - **با پایتون:**
      ```bash
      python -m http.server 3000
      ```
    - **با Node.js (serve):**
      ```bash
      npx serve .
      ```
    - **با VS Code:** استفاده از افزونه "Live Server".

3.  **برنامه را باز کنید:**
    به آدرس `http://localhost:3000` بروید.

## ⚙️ پیکربندی

پس از اولین اجرا، پنجره تنظیمات باز می‌شود:

1.  **مدیریت کلید API**: کلیدهای خود برای Gemini یا OpenAI را وارد کنید.
2.  **مدل‌های محلی (Ollama)**:
    -   در بخش "مدل سفارشی" روی "افزودن" کلیک کنید.
    -   نام مدل (مثلاً `llama3`) و آدرس (مثلاً `http://localhost:11434/v1/chat/completions`) را وارد کنید.
3.  **ذخیره‌سازی**: به صورت پیش‌فرض داده‌ها در دیتابیس مرورگر ذخیره می‌شوند. گزینه "فقط برای این نشست" داده‌ها را پس از بستن تب پاک می‌کند.

## 📁 ساختار پروژه

```
peik/
├── core/                   # هسته مستقل برنامه
│   ├── src/
│   │   ├── peik.js         # کلاس اصلی
│   │   ├── chat.js         # منطق گفتگو
│   │   ├── plugin.js       # کلاس پایه افزونه
│   │   └── ...
│
├── plugins/                # تمام افزونه‌های سیستم
│   ├── core/               # افزونه‌های ارائه‌دهنده مدل (Gemini, OpenAI, Custom)
│   ├── platform/           # افزونه‌های زیرساختی (IndexedDB, Fetch)
│   └── presentation/       # افزونه‌های نمایش (WebUI)
│       └── webUI/          # کدهای رابط کاربری وب
│
├── index.html              # نقطه ورود برنامه
└── package.json            # تعریف پروژه
```

## 🤝 مشارکت

ما از مشارکت شما استقبال می‌کنیم! از آنجایی که معماری پروژه ماژولار است، شما می‌توانید به راحتی:
-   افزونه‌های جدید برای مدل‌های دیگر بسازید.
-   رابط‌های کاربری متفاوت (مثلاً CLI) طراحی کنید.
-   افزونه‌های ذخیره‌سازی جدید (مثلاً همگام‌سازی ابری) اضافه کنید.

لطفاً برای شروع، یک `Issue` باز کنید یا `Pull Request` بفرستید.

## 📜 مجوز

این پروژه تحت مجوز MIT منتشر شده است.
