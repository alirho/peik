# گوگ (گپ و گفت)

![گوگ](https://img.shields.io/badge/Goug-v0.5.0-blue)
![زبان](https://img.shields.io/badge/Language-Persian-green)
![مجوز](https://img.shields.io/badge/License-MIT-yellow)

**گوگ** یک ابزار چت ساده، زیبا و قدرتمند با هوش مصنوعی است که برای مکالمات روان و سریع طراحی شده است. این پروژه به صورت کامل در فرانت‌اند (Frontend) و با استفاده از جاوااسکریپت خالص (Vanilla JS) پیاده‌سازی شده و از چندین ارائه‌دهنده بزرگ هوش مصنوعی پشتیبانی می‌کند.

<center><img src="/assets/images/logo.svg" alt="logo" width="80" height="80"></center>

این پروژه یک نمونه برجسته از **همکاری انسان و هوش مصنوعی** است. بخش قابل توجهی از کدها، معماری و بهبودهای این پروژه با کمک دستیارهای هوش مصنوعی توسعه یافته است تا نشان‌دهنده قدرت ابزارهای نوین در سرعت بخشیدن به فرآیند توسعه نرم‌افزار باشد.


## ✨ ویژگی‌های کلیدی

- **پشتیبانی از چندین ارائه‌دهنده**: به راحتی بین مدل‌های زیر جابجا شوید:
  - **Google Gemini**: برای استفاده از مدل‌های قدرتمند Gemini.
  - **OpenAI**: سازگار با مدل‌هایی مانند GPT-4 و GPT-3.5-Turbo.
  - **سفارشی (Custom)**: قابلیت اتصال به هر API سازگار با OpenAI، مانند [Ollama](https://ollama.com/) برای اجرای مدل‌ها به صورت محلی.
- **پشتیبانی از ارسال تصویر (چندرسانه‌ای)**:
  - **پیش‌نمایش قبل از ارسال**: تصویر انتخابی خود را قبل از فرستادن مشاهده و در صورت نیاز حذف کنید.
  - **فشرده‌سازی خودکار**: تصاویر حجیم به صورت هوشمند فشرده می‌شوند تا سرعت آپلود و مصرف حافظه بهینه شود.
  - **نمایش تمام‌صفحه**: روی تصاویر در تاریخچه گفتگو کلیک کنید تا آن‌ها را در نمای بزرگ مشاهده کنید.
- **مدیریت چندین گفتگو**: تمام گفتگوهای خود را ذخیره کرده و به راحتی بین آن‌ها جابجا شوید، نامشان را تغییر دهید یا آن‌ها را حذف کنید.
- **رونوشت آسان پیام‌ها**: با یک کلیک، متن خام (Markdown) پیام‌های دستیار یا متن پیام خود را به راحتی کپی کنید.
- **رابط کاربری مدرن و واکنش‌گرا**: طراحی تمیز و مینیمال با پشتیبانی کامل از حالت تاریک (Dark Mode).
- **استریم پاسخ‌ها در لحظه**: پاسخ‌های مدل به صورت کلمه به کلمه نمایش داده می‌شوند تا تجربه یک گفتگوی واقعی را شبیه‌سازی کنند.
- **مدیریت خطای هوشمند**:
  - **تلاش مجدد خودکار (Auto Retry)**: برای خطاهای موقتی سرور یا شبکه، درخواست به صورت خودکار با تأخیر تدریجی تکرار می‌شود.
  - **ذخیره‌سازی مقاوم**: اگر ذخیره‌سازی در مرورگر با خطا مواجه شود، برنامه به صورت خودکار در فواصل زمانی مشخص دوباره تلاش می‌کند تا از از دست رفتن داده‌ها جلوگیری شود.
- **امنیت پیشرفته**:
  - **ذخیره‌سازی محلی**: کلید API شما فقط در مرورگر شما ذخیره می‌شود.
  - **حالت نشست موقت**: می‌توانید انتخاب کنید که کلید API فقط برای نشست فعلی در حافظه (RAM) نگهداری شود و پس از بستن تب به طور کامل پاک شود.
- **بدون نیاز به بک‌اند**: کاملاً ایستا (Static) و قابل اجرا در هر محیط میزبانی وب ساده.
- **ذخیره‌سازی ماژولار**: تمام تنظیمات و تاریخچه گفتگو به صورت پایدار در مرورگر (با استفاده از IndexedDB) ذخیره می‌شود.


## 🚀 راه‌اندازی و اجرا

این پروژه به هیچ ابزار Build یا وابستگی خاصی نیاز ندارد. برای اجرای آن به صورت محلی، مراحل زیر را دنبال کنید:

1.  **مخزن (Repository) را کلون کنید:**
    ```bash
    git clone https://github.com/your-username/goug-ai-chat.git
    cd goug-ai-chat
    ```

2.  **یک سرور محلی اجرا کنید:**
    به دلیل استفاده از ماژول‌های جاوااسکریپت (`ES Modules`)، شما نمی‌توانید فایل `index.html` را مستقیماً در مرورگر باز کنید. باید آن را از طریق یک سرور وب اجرا کنید.

    - **با استفاده از VS Code و افزونه Live Server:**
      - افزونه [Live Server](https://marketplace.visualstudio.com/items?itemName=ritwickdey.LiveServer) را نصب کنید.
      - روی فایل `index.html` راست-کلیک کرده و گزینه `Open with Live Server` را انتخاب کنید.

    - **با استفاده از پایتون:**
      ```bash
      # برای پایتون 3
      python -m http.server
      ```

    - **با استفاده از Node.js:**
      ```bash
      # ابتدا پکیج serve را نصب کنید
      npm install -g serve
      # سپس پروژه را اجرا کنید
      serve .
      ```
    پس از اجرای سرور، برنامه در آدرس `http://localhost:[PORT]` در دسترس خواهد بود.


## ⚙️ پیکربندی

پس از اجرای برنامه برای اولین بار، پنجره تنظیمات به شما نمایش داده می‌شود.

1.  روی دکمه **"مدیریت کلید API"** کلیک کنید (اگر پنجره به صورت خودکار باز نشد).
2.  ارائه‌دهنده، کلید API و نام مدل خود را وارد کنید.

### نمونه تنظیمات

#### **Google Gemini**
- **ارائه‌دهنده**: `Gemini`
- **کلید API**: کلید خود را از [Google AI Studio](https://aistudio.google.com/app/apikey) دریافت کنید.
- **نام مدل**: `gemini-2.5-flash` (یا هر مدل Gemini دیگر)

#### **OpenAI**
- **ارائه‌دهنده**: `OpenAI`
- **کلید API**: کلید خود را از [داشبورد OpenAI](https://platform.openai.com/api-keys) دریافت کنید.
- **نام مدل**: `gpt-4`, `gpt-3.5-turbo`

#### **سفارشی (مثال: Ollama)**
برای اجرای مدل‌ها به صورت محلی با [Ollama](https://ollama.com/).
- **ارائه‌دهنده**: `سفارشی`
- **کلید API**: هر مقداری قابل قبول است (مثلاً `ollama`).
- **نام مدل**: نام مدلی که در Ollama دارید (مثلاً `llama3`).
- **آدرس API**: `http://localhost:11434/v1/chat/completions`

## 📁 ساختار پروژه

پروژه دارای یک معماری ماژولار و تمیز است تا توسعه و نگهداری آن آسان باشد:

```
goug/
├── index.html              # فایل ورودی اصلی
│
├── styles/                 # استایل‌های ماژولار
│   ├── ... (فایل‌های CSS)
│
├── js/
│   ├── core/               # منطق اصلی برنامه
│   │   ├── chatEngine.js   # ارکستراتور اصلی هسته
│   │   ├── eventEmitter.js # سیستم مدیریت رویداد
│   │   ├── modules/        # ماژول‌های تخصصی هسته
│   │   │   ├── chatManager.js
│   │   │   ├── messageHandler.js
│   │   │   ├── storageManager.js
│   │   │   └── syncManager.js
│   │   └── providers/      # منطق مخصوص هر ارائه‌دهنده API
│   │       ├── geminiProvider.js
│   │       ├── ...
│   ├── services/           # سرویس‌های خارجی (API, Storage)
│   │   ├── apiService.js
│   │   └── indexedDBStorage.js # پیاده‌سازی IndexedDB
│   ├── ui/                 # منطق رابط کاربری (UI)
│   │   ├── chatUI.js       # ارکستراتور اصلی UI
│   │   ├── templateLoader.js # بارگذاری قالب‌های HTML
│   │   └── components/     # کامپوننت‌های UI تخصصی
│   │       ├── inputManager.js
│   │       ├── fileManager.js
│   │       ├── messageRenderer.js
│   │       ├── sidebarManager.js
│   │       └── ...
│   ├── utils/              # ابزارهای کمکی
│   │   └── constants.js
│   └── main.js             # نقطه ورود اصلی JavaScript
│
└── templates/              # قالب‌های HTML ماژولار
    ├── mainLayout.html     # ساختار اصلی
    ├── settingsModal.html
    └── partials/           # قطعات قابل استفاده مجدد
        ├── header.html
        ├── sidebar.html
        └── ...
```


## 🤝 مشارکت

از مشارکت شما استقبال می‌کنیم! اگر ایده‌ای برای بهبود پروژه دارید، لطفاً یک `Issue` باز کنید یا یک `Pull Request` ارسال کنید.

1.  پروژه را **Fork** کنید.
2.  یک شاخه (Branch) جدید برای ویژگی خود بسازید (`git checkout -b feature/AmazingFeature`).
3.  تغییرات خود را **Commit** کنید (`git commit -m 'Add some AmazingFeature'`).
4.  شاخه خود را به مخزن اصلی **Push** کنید (`git push origin feature/AmazingFeature`).
5.  یک **Pull Request** باز کنید.


## 📜 مجوز

این پروژه تحت مجوز MIT منتشر شده است. برای اطلاعات بیشتر فایل `LICENSE` را مطالعه کنید.