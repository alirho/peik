# پرامپت‌های هوش

## پرامپت‌های طراحی

### پرامپت ۱
می‌خوام برام یک ابزار چت با مدل‌های هوش پیاده‌سازی کنی.

### پرامپت ۲
می‌خوام طرح را برای زبان فارسی بهینه‌سازی کنی.

## پرامپت‌های پیاده‌سازی

### پرامپت ۱
می‌خوام یک ابزار گپ (chat) با مدل‌های هوش مصنوعی با نام گوگ (goug) بسازی که ویژگی‌های زیر را داشته باشه:
1. فقط با جاوااسکریپت خام (بدون فریمورک)
2. بتونه به OpenAI متصل بشه
3. پیام‌ها را ذخیره کنه (localStorage)
4. یک رابط کاربری ساده داشته باشه
5. کاربر API key خودش را وارد کنه (یک input ساده)
6. پیام‌ها بعد از رفرش صفحه باقی بمونن
7. طراحی ساده و تمیز (فارسی و راست‌چین)
8. کدها خوانا و کامنت‌دار باشن
9. فقط یک مکالمه (نه لیست مکالمه‌ها)

### پرامپت ۲
خب می‌خوام پروژه را بدون استفاده از هیچ چارچوبی (framework) پیاده‌سازی کنی.
- از react استفاده نکن.
- فقط از pure javascript استفاده کن.
- نمی‌خوام در پروژه هیچ فایلی با پسوندهای tsx و ts وجود داشته باشه.

### پرامپت ۳
در کنسول مرورگر خطای زیر نمایش داده می‌شه:

```js
Uncaught TypeError: The specifier “@google/genai” was a bare specifier, but was not remapped to anything. Relative module specifiers must start with “./”, “../” or “/”.
```

### پرامپت ۴
حالا می‌خوام همین کد را به یک **معماری ماژولار و حرفه‌ای** تبدیل کنی.
1. هر فایل مسئول یک وظیفه باشه.
2. از کتابخانه tailwind استفاده نکن و بجاش از CSS استفاده کن.
3. جداسازی کامل: Core نباید UI را بشناسه. UI از Core از طریق API استفاده کنه.
4. بدون کتابخانه خارجی: حذف `@google/genai`
5. استفاده از ES6 Modules
    - export/import استاندارد   
    - هر فایل یک ماژول
6. ویژگی‌های فعلی مثل Streaming response و Auto-resize textarea و Loading state را حفظ کن
7. معماری Event-Driven
    - ChatEngine رویداد emit کنه
    - ChatUI به رویدادها گوش بده

> پس از تغییر معماری پروژه، پاسخ مدل gemini نمایش داده نمی‌شد. با کمک مدل sonnet و لاگ‌گذاری در کد و پیدا کردن خطا مشکل بر طرف شد.

### پرامپت ۵
زمانی که صفحه را بازنشانی (refresh) می‌کنم، فقط پیام‌های مدل هوش را نمایش می‌ده و پیام‌های ارسالی من را نمایش نمی‌ده.

### پرامپت ۶
می‌خوام بهبودهای زیر را در پروژه اعمال کنی:
1. مدیریت بهتر Stream (Buffer): اگر یک پاسخ وسط JSON قطع بشه، خطا می‌ده، یک متغیر `buffer` بساز که خطوط ناتمام را نگه داره، هر خط کامل را پردازش کن، خط آخر ناقص را برای chunk بعدی نگه دار
2. پیام‌های خطای واضح: الان همه خطاها یک شکل نمایش داده می‌شن
    - `401`: "کلید API اشتباه است"
    - `429`: "درخواست‌های زیاد. لطفاً 1 دقیقه صبر کنید"
    - `400`: "درخواست نامعتبر"
    - `500`: "مشکل در سرور، دوباره تلاش کنید"
    - خطای شبکه: "اینترنت قطع است"
3. تلاش مجدد خودکار (Retry): اگر درخواست fail شد، 3 بار دوباره تلاش کنه
    - هر بار تأخیر بیشتری داشته باشه (1 ثانیه، 2 ثانیه، 4 ثانیه)
    - فقط برای خطاهای موقت retry کنه (مثل 500، network error)
    - برای خطاهای دائمی retry نکنه (مثل 401، 400)
4. محدودیت زمانی (Timeout): اگر درخواست بیشتر از 30 ثانیه طول کشید، لغوش کنه
    - پیام بده: "درخواست خیلی طول کشید، دوباره تلاش کنید"
5. نمایش بهتر خطا در UI: به جای `alert()` یک پیام رنگی در خود چت نشان بده
    - پیام خطا قرمز رنگ باشه و بعد از 5 ثانیه محو بشه
نکات مهم:
- کدهای فعلی رو خراب نکن، فقط بهبودشون بده
- کامنت‌های فارسی برای بخش‌های جدید بنویس
- ساده و خوانا بنویس

### پرامپت ۷
الان فقط Gemini پشتیبانی می‌شه. می‌خوام کاربر بتونه بین مدل‌های مختلف انتخاب کنه.
1. انتخاب نوع مدل
    - **Gemini** (فعلی)
    - **OpenAI** (GPT-4, GPT-3.5, و...)
    - **سفارشی** (API خودش)
    - باید مثل OpenAI باشه (استاندارد OpenAI-compatible).
2. تنظیمات مدل
    - **کلید API** (برای همه)
    - **نام مدل** (مثلاً gpt-4، gpt-3.5-turbo)
    - **آدرس API** (فقط برای سفارشی)
      - مثال: `http://localhost:11434/v1/chat/completions` (Ollama)
      - مثال: `https://api.openai.com/v1/chat/completions`
3. تنظیمات باید ذخیره بشن
    - نوع مدل (gemini/openai/custom)
    - کلید API
    - نام مدل
    - آدرس API (اگر سفارشی باشه)

نکات مهم:
1. **کد فعلی را خراب نکن**: Gemini همچنان کار کنه
2. **UI ساده**: فرم تنظیمات قشنگ و ساده باشه
3. **Validation**: اگر آدرس API غلط باشه، خطا بده
4. **کامنت‌ها**: برای بخش‌های جدید کامنت فارسی بنویس
5. **Error handling**: خطاهای OpenAI را هم مدیریت کن
6. دقت کن معماری پروژه که ماژولار و هر فایل یک وظیفه را رعایت کن.

### پرامپت ۸
یک README.md حرفه‌ای برای پروژه گوگ بنویس. در این سند بگو که پروژه با استفاده از هوش مصنوعی نوشته شده.

> پس از دریافت طرح‌ها و کد‌های پایه از stitch، کدهای را به مدل‌های sonnet4.5 و gemini 2.5 pro تا کتابخانه tailwind را از کدهای پایه حذف کنند.
> با توجه به اینکه کدهای اصلاح شده sonnet 4.5 بسیار نزدیک‌تر به طرح بود آن‌ها را برای پیاده‌سازی به مدل هوش دادم.

### پرامپت ۹
من کدهای طرح جدید UI برای پروژه گوگ را بارگذاری کردم. می‌خوام:
1. این طرح را جایگزین طرح فعلی کن
2. کلاس‌ها و IDها را با کد JavaScript فعلی تطبیق بده
3. مودال تنظیمات را در این طرح جدید بساز (فایل key.html)
نکات مهم:
- کد JavaScript فعلی را خراب نکن
- فقط UI را تغییر بده
- تمام قابلیت‌های فعلی را حفظ کن
- صفحه گپ (chat) و مودال کلیدها را طبق طرح جدید بساز

### پرامپت ۱۰
ازت می‌خوام تغییرات زیر را اعمال کنی:
1. در صفحه گپ خطی که بالای بخش نوشتن کاربر هست را حذف کن.
2. رنگ پنل کناری (sidebar) با چیزی که در طرح‌ها فرستادم یکسان نیست. رنگش با بقیه صفحه یکسانه که درست نیست.
3. دقت کن اسم پروژه را باید فارسی بنویسی «گوگ»
4. ازت می‌خوام مدال مدیریت کلیدها را دقیقا مشابه طرح پیاده‌سازی کنی.

### پرامپت ۱۱
خب با تغییراتی که دادی دکمه مدیریت کلیدها در پنل سمت راستی شبیه دکمه نیست، البته وقتی ماوس روش قرار می‌گیره به صورت دکمه نمایش داده می‌شه.

### پرامپت ۱۲
حالا ازت می‌خوام بخش مدیریت کلیدها را دقیقا مشابه کدی که بارگذاری کردم پیاده‌سازی کنی بدون هیچ تغییری.

### پرامپت ۱۳
بخشی که کاربر متن خودش را وارد می‌کنه با بخش نمایش پیام‌ها در یک راستا نیست. در سمت راست مشکلی نیست ولی در سمت چپ متن‌ها نمایش داده شده و فیلد ورود متن با هم در یک راستا نیستن.

عکس مشکل را پیوست کردم.

### پرامپت ۱۴
تغییری که دادی مشکل را حل نکرد.

### پرامپت ۱۵
تغییری که ایجاد کردی باعث شد ناترازی به سمت راست منتقل بشه.

### پرامپت ۱۶
بخش گپ به درستی پیاده‌سازی نشده، پیام‌های نمایش داده شده با فیلد وردی در یک راستا نیستن.
1. پیام‌هایی که کاربر می‌نویسه از سمت چپ صفحه فاصله دارن
2. پیام‌های مدل هوش هم از سمت راست صفحه فاصله دارن.

در صورتیکه باید به این شکل باشه:
1. پیام کاربر باید سمت راست نمایش داده بشه و انتهای پیام باید از سمت چپ صفحه فاصله داشته باشه.
2. پیام مدل هوش هم باید سمت چپ نمایش داده و انتهای پیام از سمت راست صفحه فاصله داشته باشه.

### پرامپت ۱۷
تغییرات را کامل انجام ندادی.
1. آواتار کاربر به سمت راست منتقل نشده و جای قبلیش باقی مونده
2. آواتار هوش هم باید به سمت چپ پیام هوش منتقل بشه

### پرامپت ۱۸
ساختار فایل‌های HTML و CSS پروژه پیچیده شده، می‌خوام این فایل‌ها را به یک معماری ماژولار و مبتنی بر کامپوننت تبدیل کنی تا نگهداری و توسعه اون‌ها آسان‌تر بشه.

1.  **ساده‌سازی `index.html`:** این فایل باید بسیار سبک بشه و فقط شامل یک المنت ریشه در `<body>` باشه.
2.  **ایجاد کامپوننت‌های HTML:** یک پوشه جدید به نام `templates/` بساز.
    - هر بخش از HTML باید یک قالب جدا باشه.
3.  بارگذاری پویای کامپوننت‌ها
4.  فایل `chatUI.js` را هم باید ماژولار کنی.
5.  برای **استایل‌ها** این موارد را رعایت کن
    - حداکثر 100 خط در هر فایل CSS
    - نام‌گذاری BEM یا مشابه
    - هیچ استایل inline نباشه
6. برای فایل‌های **JavaScript**:
   - حداکثر 200 خط در هر فایل
   - Export/Import صحیح
   - JSDoc برای توابع
7. برای فایل‌های **HTML**:
   - Templates در فایل جداگانه یا template literals
   - Semantic HTML
   - Accessibility
8.  **جداسازی Core/UI**: دقت کن که ظاهر باید از منطق برنامه جدا باشه و وابستگی نداشته باشن
     - Core هیچ DOM manipulation نکند
     - UI فقط از Core API استفاده کند
     - Event-driven communication
9.  ساختار پیشنهادی:

```
goug/
├── index.html
│
├── styles/
│   ├── variables.css        # CSS Variables
│   ├── base.css             # Reset, Typography
│   ├── layout.css           # Layout اصلی (sidebar, main)
│   ├── components/
│   │   ├── modal.css        # Modal styles
│   │   ├── sidebar.css      # Sidebar styles
│   │   ├── messages.css     # Message bubbles
│   │   ├── input.css        # Input area
│   │   └── buttons.css      # Button styles
│   └── utilities.css        # Utilities (spinner, typing, error)
│
├── js/
│   ├── ui/
│   │   ├── chatUI.js
│   │   └── components/
│   │       ├── settingsModal.js     # مدیریت modal
│   │       ├── messageRenderer.js   # رندر پیام‌ها
│   │       └── sidebarManager.js    # مدیریت sidebar
│   └── main.js
│
└── templates/              # HTML Templates
    ├── settingsModal.html
    └── sideBar.html
```
این موارد را هم حتما رعایت کن:
- **Single Responsibility**: هر فایل یک مسئولیت
- **Reusability**: کامپوننت‌های قابل استفاده مجدد
- **Maintainability**: کد قابل نگهداری

### پرامپت ۱۹
اینکه همه فایل‌های css را در html آوردی به نظرم خوب نیست، بهتره فقط یک فایل را در این بخش معرفی کنی.

### پرامپت ۲۰
حالا ازت می‌خوام بخش منطق پروژه را هم ماژولار کنی، مشابه کاری که با فایل‌های `chatUI.js` و css کردی.

1. هر provider یک فایل جداگانه
2. UI components جدا از core
3. **Separation of Concerns**: UI کاملاً جدا از Core
4. **Single Responsibility**: هر فایل یک مسئولیت
5. **Reusability**: کامپوننت‌های قابل استفاده مجدد
6. **Maintainability**: کد قابل نگهداری
7. در فایل‌های **JavaScript**موارد زیر را رعایت کن:
   - حداکثر 200 خط در هر فایل
   - Export/Import صحیح
   - JSDoc برای توابع

ساختار پشنهادی:
```
goug/
├── index.html
├── styles/
├── js/
│   ├── core/
│   │   ├── chatEngine.js
│   │   ├── eventEmitter.js
│   │   └── providers/
│   │       ├── geminiProvider.js    # جداسازی هر provider
│   │       ├── openaiProvider.js
│   │       └── customProvider.js
│   ├── services/
│   │   ├── apiService.js
│   │   └── storageService.js
│   ├── ui/
│   ├── utils/
│   └── main.js
└── templates/
```

### پرامپت ۲۱
ازت می‌خوام یک پوشه ایجاد کنی و سندهای زیر را در اون بنویسی:
1. معماری
    - نمودار معماری (با متن)
    - توضیح هر لایه (UI, Core, Storage)
    - Event-driven architecture چطور کار می‌کنه
    - نمودار جریان یک درخواست از ابتدا تا انتها
    - تصمیمات طراحی و چرایی آن‌ها
    - Dependency diagram
    - چطور ماژول‌ها با هم ارتباط دارند
    - Modular
    - Separation of concerns
2. فایل برای توسعه‌دهنده‌گان
    - بخش 1: API داخلی
      - تمام eventهایی که ChatEngine emit می‌کنه
      - تمام متدهای public در هر کلاس
      - نحوه استفاده از هر API با مثال
    - بخش 2: راهنمای توسعه
      - چطور یک provider جدید اضافه کنیم
      - چطور UI سفارشی بسازیم
      - چطور storage mechanism را تغییر دهیم
      - نحوه اضافه کردن plugin
    - Best Practices
      - نکات کدنویسی
      - الگوهای طراحی استفاده شده
      - نکات امنیتی

این سندها باید فنی اما قابل فهم برای توسعه‌دهندگان باشه. به فارسی هم بنویس.

### پرامپت ۲۲
سند توسعه دهنده‌گان را ننوشتی.

### پرامپت ۲۳
متن داخل دکمه‌ها از فونت وزیر متن استفاده نمی‌کنند.

### پرامپت ۲۴
با هر بار بازنشانی صفحه (refresh) متن‌های پیشین در گپ پاک می‌شن.

### پرامپت ۲۵
می‌خواهم قابلیت مدیریت چند گپ (chat) را اضافه کنی.
1. با زدن دکمه گپ جدید یک مکالمه کاملاً جدید و خالی ایجاد بشه.
    - این گپ جدید باید به صورت خودکار به عنوان گپ فعال انتخاب بشه و در بالای لیست گپ‌ها در نوار کناری نمایش داده بشه.
2. نمایش لیست گپ‌ها در نوار کناری (Sidebar):
    - نوار کناری باید لیستی از تمام گپ‌ها موجود را نمایش بده.
    - هر آیتم در این لیست باید عنوان آن گپ را نشون بده.
    - گپی که در حال حاضر فعاله، باید از نظر ظاهری متمایز باشه.
    - وقتی کاربر روی یک گپ در لیست کلیک می‌کنه، محتوای اون گپ در پنجره اصلی گپ بارگذاری و نمایش داده بشه.
3. مدیریت عنوان گپ وقتی کاربر نسختین پیام خود را در یک گپ جدید ارسال می‌کنه، همان متن پیام (مثلاً ۳۰ نویسه نخست) به صورت خودکار به عنوان "عنوان" آن گپ تنظیم بشه.
    - کاربر باید بتونه عنوان گپ را ویرایش کنه.
5. کاربر بتونه مکالمه رو حذف کنه، برای حذف حتما از کاربر تاییدیه بگیر.
    - اگر کاربر تأیید کرد، مکالمه باید به طور کامل از سیستم (و از حافظه ذخیره‌سازی) حذف بشه. اگر مکالمه حذف شده، مکالمه فعال بود، یک گپ خالی فعال بشه.
6. گزینه‌های ویرایش نام و حذ در یک لیست برای هر گپ در نوار کناری نمایش داده بشه. (سه نقطه افقی)
7. برای هر گپ موارد زیر باید ذخیره بشه
    - تاریخ ایجاد
    - تاریخ آخرین به‌روزرسانی
    - فراهم کننده و نام مدل

### پرامپت ۲۶
تغییراتی که برای مدیریت گپ‌ها ایجاد کردی مشکل‌های زیر را داره:
1. دکمه گپ جدید بالا قرار گرفته، در حالیکه باید همون پایین در جای پیشین باشه.
2. نام دکمه «چت جدید» باید بشه «گپ جدید»
3. گپ فعال در پنل کناری از سایر گپ‌ها متمایز نشده (باید رنگش تغییر کنه.)
4. برای ویرایش نام و حذف باید مدال مناسب با پروژه طراحی کنی.

### پرامپت ۲۷
خب بخش لوگوی «گوگ» را هم برگردون.

### پرامپت ۲۸
در چند جا از پروژه، محتوای دریافتی از کاربر یا API به صورت مستقیم در HTML قرار می‌گیرد که می‌تواند باعث حملات XSS شود.
1. فایل: `js/ui/components/sidebarManager.js`
    - در متد `handleRename`:
       - خط مربوط به `bodyHtml` که مقدار `chat.title` را در attribute قرار می‌دهد
       - راه‌حل: مقدار را بعد از ساخت input تنظیم کن، نه در HTML
    - در متد `handleDelete`:
       - خط مربوط به `bodyHtml` که `chat.title` را در تگ `<strong>` قرار می‌دهد
       - راه‌حل: ابتدا یک تابع کمکی برای escape کردن HTML بنویس و از آن استفاده کن
یک متد به نام `escapeHtml` به کلاس `SidebarManager` اضافه کن که متن را از کاراکترهای خطرناک HTML پاک کند.
2. فایل: `js/ui/components/messageRenderer.js`
    - در متد `displayTemporaryError`:
       - خط استفاده از `innerHTML` برای نمایش `errorMessage`
       - راه‌حل: از `textContent` استفاده کن یا المان را به صورت دستی بساز

> پرامپت بالایی پس از بررسی کدها توسط مدل sonnet نوشته شده است.

### پرامپت ۲۹
حالت `disabled` برای دکمه‌ها تعریف نشده.

### پرامپت ۳۰
بهبود مدیریت خطا در EventEmitter، اگر یکی از listener ها خطا داشته باشه، بقیه listener ها اجرا نمی‌شن و برنامه ممکن متوقف بشه.

### پرامپت ۳۱
1. رفع مشکل RTL در Input Group، در بخش input group، استایل‌ها فقط برای LTR نوشته شده و در حالت RTL (فارسی) درست نمایش داده نمی‌شه. برای حالت `[dir="rtl"]` استایل‌های مخصوص اضافه کن که `border-radius` و `border` را صحیح قرار بده.
2. اضافه کردن مدیریت خطا در main.js، اگر در هنگام بارگذاری خطایی رخ بده، کاربر صفحه خالی یا پیام خطای مبهم می‌بینه.
3. اضافه کردن Focus State برای دسترسی‌پذیری، 
    - فایل: `styles/components/buttons.css`: کاربرانی که با کیبورد کار می‌کنند نمی‌توانند ببینند کدام دکمه فوکوس شده است.
      - برای حالت `:focus-visible` یک outline آبی اضافه کن.
    - فایل: `styles/components/sidebar.css`، وقتی کاربر با Tab به یک chat item می‌رسد، دکمه سه نقطه نمایش داده نمی‌شود.
      - برای حالت `:focus-within` در `.chat-list-item`، دکمه actions را نمایش بده.

### پرامپت ۳۲
یک فایل کامل و جامع به نام `docs/USER_GUIDE.md` ایجاد کن که راهنمای استفاده از گوگ برای کاربران نهایی باشد.
- **نام پروژه:** گوگ (Goug)  
- **توضیح:** ابزار متن‌باز گفت‌وگو با مدل‌های هوش مصنوعی
- **ویژگی کلیدی:** کاملاً سمت کاربر اجرا می‌شود، نیازی به سرور ندارد، همه داده‌ها در مرورگر کاربر ذخیره می‌شود.

ساختار راهنما:
1. معرفی
    - گوگ چیست؟
    - چه مشکلی را حل می‌کند؟
    - چرا از گوگ استفاده کنیم؟ (مزایا)
2. شروع سریع (5 دقیقه)
3. تنظیمات اولیه
    - دریافت API Key
4. کار با گپ‌ها (Chats)
5. ارسال و دریافت پیام
    - نوشتن پیام در کادر پایین صفحه
    - فشردن Enter برای ارسال (Shift+Enter برای خط جدید)
    - مشاهده پاسخ به صورت تدریجی (streaming)
    - نمایش "در حال تایپ..." هنگام دریافت پاسخ
6. ذخیره‌سازی و حریم خصوصی
7. میانبرهای کیبورد
- Enter: ارسال پیام
- Shift+Enter: خط جدید
- Tab: جابجایی بین المان‌ها
8. رفع مشکلات رایج (Troubleshooting)
    - مشکل: پیام خطا "تنظیمات API صحیح نیست"**
    - مشکل: پاسخ دریافت نمی‌شود**
    - مشکل: گپ‌ها پاک شدند
    - مشکل: برنامه بارگذاری نمی‌شود
9. پرسش‌‌های پرتکرار (FAQ)
    - آیا گوگ رایگان است؟
    - آیا داده‌های من امن است؟
    - آیا می‌توانم در موبایل استفاده کنم؟
    - چگونه می‌توانم از گپ‌ها نسخه پشتیبان تهیه کنم؟
    - آیا می‌توانم چند API Key داشته باشم؟
10. پشتیبانی و کمک


سبک نگارش سند:
- زبان ساده و روان
- فارسی رسمی اما صمیمی
- مثال‌های واقعی و کاربردی
- تصاویر توضیحی نیازی نیست (فقط متن)
- جدول برای مقایسه یا لیست‌ها

### پرامپت ۳۳
می‌خوام پیام‌های دریافتی از مدل‌های هوش مصنوعی که به صورت Markdown هستند، با قالب‌بندی صحیح نمایش داده شوند.

1. از کتابخانه **Markdown-it** استفاده کن.
2. کتابخانه را به صورت محلی استفاده کن
3. رندر محتوا
    - پیام‌های کاربر (user) به صورت متن ساده باقی بمانند
    - فقط پیام‌های مدل (assistant) با Markdown رندر شوند
    - هنگام streaming (نمایش تدریجی پاسخ)، Markdown به درستی رندر شود
    - پیام خوش‌آمدگویی هم با Markdown رندر شود
4. برای المان‌های Markdown استایل مناسب اضافه کن.
5. آماده‌سازی برای آینده: معماری باید طوری باشد که بتوان مفسر Markdown را در آینده راحت تعویض کرد (برای استفاده از مفسر سفارشی فارسی).
6. استایل‌ها باید با متن‌های راست‌چین (فارسی) سازگار باشند.

**نکات مهم:**
- استایل‌ها باید با زبان فارسی سازگار باشند (RTL)
- در حالت Dark Mode هم درست نمایش داده بشن
- بلوک‌های کد باید LTR باشند (چپ‌چین)
- لیست‌ها و blockquote ها از سمت راست padding داشته باشن
- معماری ماژولار پروژه و اینکه هر فایل یک وظیفه انجام بده را حفظ کن.

### پرامپت ۳۴
در کنسول مرورگر خطای زیر نمایش داده می‌شه:

```js
Uncaught SyntaxError: The requested module 'http://127.0.0.1:8000/js/lib/markdown-it.min.js' doesn't provide an export named: 'default'
```

### پرامپت ۳۵
من فایل لوگوی پروژه را در مسیر `assets/images/logo.svg` قرار دادم. ازت می‌خوام این لوگو را در نوار کناری پیش از نام پروژه قرار بدی. همینطور به عنوان favicon هم استفاده کنی.

### پرامپت ۳۶
در حال حاضر مودال مدیریت کلیدها با هر بار ورود به برنامه نمایش داده می‌شه که آزاردهنده است.
1.  **نمایش هوشمند مودال:** مودال تنظیمات فقط باید در صورتی به صورت خودکار نمایش داده بشه که **برنامه برای اولین بار اجرا می‌شه و هنوز هیچ کلید API معتبری ذخیره نشده.**
2.  **عدم نمایش در بازدیدهای بعدی:** اگر کاربر قبلاً کلید API خود را وارد و ذخیره کرده، برنامه باید به طور عادی و بدون نمایش مودال تنظیمات شروع به کار کنه.
3.  **دسترسی دستی:** کاربر همچنان باید بتواند با کلیک کردن روی دکمه "مدیریت کلید API" (یا مشابه آن)، به صورت دستی مودال تنظیمات را باز کنه.
4. **اعتبارسنجی:** حداقل باید Provider و کلید API هر دو موجود باشن
5. اگر کاربر Cache مرورگر را پاک کنه، تنظیمات پاک می‌شه و دوباره مودال نمایش داده می‌شه (رفتار درست)
6. **پر کردن خودکار فرم:** وقتی مودال تنظیمات باز می‌شه (چه به صورت خودکار و چه با کلیک کاربر)، فیلدهای فرم (مثل کلید API، ارائه‌دهنده، نام مدل و...) باید به صورت خودکار با **آخرین مقادیر ذخیره شده** پر بشن.

### پرامپت ۳۷
همچنان با بازنشانی (refresh) صفحه مدال نمایش داده می‌شه. البته اطلاعات ذخیره شده به خوبی نمایش داده می‌شه.

دقت کن اگر کاربر اطلاعات کلید (API key) را قبلا وارد کرده نباید مودال نمایش داده بشه.

### پرامپت ۳۸
ازت می‌خوام داده‌های گوگ را `indexedDB` ذخیره کنی. دقت کن که برای هر گپ می‌خوام داده‌های زیر حتما ذخیره بشن:
1. نام گپ
2. تاریخ ایجاد گپ
3. تاریخ آخرین بروزرسانی
4. نام مدل
5. فراهم کننده مدل

### پرامپت ۳۹
چند مشکل مهم در سیستم ذخیره‌سازی فعلی وجود داره که باید رفع بشن:
1. الان هر بار که یک گپ تغییر می‌کنه (مثلاً پیام جدید، تغییر نام)، **همه چت‌ها** دوباره در IndexedDB نوشته می‌شن، که بهینه نیست.
    - متدهای تازه برای بروزرسانی و حذف یک گپ ایجاد کن.
    - دقت کن که این کار را در کل پروژه انجام بدی
    - متد فعلی را نگهدار
2. اگر IndexedDB در مرورگر کاربر غیرفعال باشه یا خطا بده، برنامه crash می‌کنه.
     - پیام مناسب برای خطاها نمایش بده
3. مدیریت محدودیت حجم (QuotaExceeded)، اگر فضای ذخیره‌سازی مرورگر پر بشه، خطای QuotaExceeded رخ می‌ده و کاربر متوجه نمی‌شه.
    - به کاربر هشدار نمایش بده: "فضای ذخیره‌سازی مرورگر پر است. لطفاً گپ‌های قدیمی را حذف کنید."
    - برای خطاهای دیگر: log کن و به کاربر خطای عمومی نمایش بده
    - قبل از ذخیره، حجم تخمینی داده‌ها را بررسی کن، اگر حجم بیش از 45 مگابایت بود:
      - یک console.warn نمایش بده: "هشدار: نزدیک به حد مجاز ذخیره‌سازی هستید"
4. مدیریت Concurrent Tabs (تب‌های همزمان)، اگر کاربر دو تب از گوگ باز کنه و در یکی گپ جدید بسازه، تب دیگر متوجه نمی‌شه.

دقت کن پیام‌های خطا باید به زبان فارسی و قابل فهم باشن.

### پرامپت ۴۰
ازت می‌خوام براساس تغییرهایی که دادی که سندهای مورد نیاز را بروزرسانی کنی.

### پرامپت ۴۱
می‌خوام ویژگی **بارگذاری تصویر** را پیاده‌سازی کنی:
1. **دکمه بارگذاری:** یک دکمه با آیکون "گیره کاغذ" (📎) کنار کادر ورود متن اضافه بشه.
2. **انتخاب فایل:** با کلیک روی این دکمه، کاربر بتونه **فقط یک فایل تصویر** (مثل PNG, JPG, GIF) از کامپیوترش انتخاب کنه.
3. **محدودیت حجم:** اگر حجم فایل بیشتر از **۱۰ مگابایت** بود، یک پیام خطا به کاربر نشون بده و فایل رو قبول نکن.
4. **پیش‌نمایش:** بعد از انتخاب تصویر، یک پیش‌نمایش کوچک از اون زیر کادر متن ظاهر بشه. کنار این پیش‌نمایش یک دکمه "حذف" (❌) هم باشه تا کاربر بتونه انتخابش رو لغو کنه.
5. **ارسال به مدل:** وقتی کاربر پیام رو ارسال می‌کنه، هم متن و هم تصویر با هم برای مدل هوش مصنوعی ارسال بشه.
6. **نمایش در تاریخچه:** در پنجره چت، پیام ارسال شده باید هم تصویر و هم متن کاربر رو نمایش بده.
7.  **ذخیره‌سازی:** تصویر باید همراه با تاریخچه گفتگو ذخیره بشه تا بعد از باز کردن دوباره برنامه، حذف نشه.
8.  خطاها را به درستی مدیریت کن و پیام مناسب به کاربر نمایش بده
9.  عملکرد نباید تغییر کند - کاربر همچنان می‌تواند بدون تصویر هم پیام بفرسته

**توجه:** این ویژگی باید با ساختار و معماری فعلی پروژه سازگار باشد و تغییرات لازم در تمام لایه‌ها اعمال بشه.

### پرامپت ۴۲
تغییرات زیر را انجام بده:
1. اگر تصویری در پیام‌ها به درستی بارگذاری نشد (خطای load)، به جای تصویر یک متن "⚠️ خطا در بارگذاری تصویر" نمایش بده تا کاربر متوجه مشکل بشه.
2. در تمام جاهایی که از `message.image` استفاده می‌شه، قبلش چک کن که هم `image.data` و هم  image.mimeType` وجود داشته باشن. اگه نداشتن، تصویر رو نادیده بگیر و فقط متن رو نمایش بده.
3. اگر تصویر در بخش پیش‌نمایش (preview) به درستی بارگذاری نشد، پیش‌نمایش رو حذف کن و یک پیام خطا به کاربر نشون بده: "خطا در نمایش پیش‌نمایش تصویر"

### پرامپت ۴۳
اگر کاربر تصویری با حجم بیشتر از 2 مگابایت انتخاب کرد، قبل از ذخیره کردن، اون رو فشرده‌سازی کن:
- عرض تصویر رو به حداکثر 1200 پیکسل کاهش بده (با حفظ نسبت ابعاد)
- کیفیت رو به 80% تنظیم کن

این کار باعث میشه حجم کمتری در پایگاه‌داده ذخیره بشه.

### پرامپت ۴۴
سه بهبود جزئی برای فشرده‌سازی تصویر:
1. GIF‌های انیمیشنی رو فشرده نکن، چون انیمیشن‌شون از بین میره. اگه فایل از نوع image/gif بود، بدون فشرده‌سازی ذخیره کن.
2. تصاویر عمودی (مثل 800×2000) هم فشرده بشن. الان فقط عرض چک میشه، باید بزرگترین ضلع (عرض یا ارتفاع) رو به حداکثر 1200 پیکسل محدود کنی.
3. اگه تصویر اصلی PNG بود، بعد از فشرده‌سازی هم PNG نگهش دار (نه JPEG)، چون کیفیت بهتری داره.

### پرامپت ۴۵
در فایل Gemini Provider، وقتی خطا رخ میده، سعی کن پیام خطای دقیق رو از `response.json()` بخونی و به کاربر نشون بدی. اگه نتونستی، همون پیام پیش‌فرض رو نمایش بده.

(دقیقاً مثل OpenAI Provider که یک تابع `getOpenAIErrorMessage` داره)

### پرامپت ۴۶
کارهای زیر را انجام بده:
1. وقتی تصویری در پیام‌ها در حال بارگذاری است، یک انیمیشن لودینگ (مثل یک مستطیل خاکستری با افکت shimmer) نمایش بده. وقتی تصویر کامل لود شد، انیمیشن رو حذف کن.
2. وقتی کاربر روی تصویری در پیام‌ها کلیک می‌کنه، تصویر رو در یک نمای تمام‌صفحه (با پس‌زمینه تیره) نمایش بده. با کلیک دوباره روی پس‌زمینه، نمای بزرگ بسته بشه.
3. در OpenAI Provider، اگر کاربر فقط تصویر فرستاده و متن نداشته، بخش text رو اصلاً به API نفرست. فقط بخش image_url رو بفرست.

### پرامپت ۴۷
می‌خوام نمایش تمام صفحه عکس با دکمه Esc هم بسته بشه.

### پرامپت ۴۸
ازت می‌خوام براساس تغییراتی که دادی و ویژگی بارگذاری تصویر، تمامی سندها را برورزرسانی کنی.

### پرامپت ۴۹
می‌خوام یک دکمه رونوشت (Copy) کنار هر پیام اضافه کنی:
1. کنار هر پیام (کاربر و مدل) یک دکمه کوچک با آیکون "رونوشت" اضافه بشه.
2. این دکمه فقط در حالت hover نمایش داده بشه.
3. وقتی کاربر کلیک می‌کنه:
   - متن پیام (message.content) رونوشت بشه
   - یک آیکون "✓" با رنگ سبز برای ۳ ثانیه نمایش داده بشه
4. برای پیام‌های مدل که Markdown دارن، متن اصلی (قبل از render) کپی بشه.

### پرامپت ۵۰
آیکون نمایش رونوشت در کنار پیام کاربر نمایش داده نمی‌شه و به سمت چپ صفحه نمایش داده می‌شه.

 ### پرامپت ۵۱
 می‌خوام به هر پیام یک شناسه یکتا (ID) اضافه کنی:
1. هر پیامی که ساخته می‌شه (چه از کاربر، چه از مدل) باید یک فیلد id داشته باشه که یک رشته یکتا باشه.
2. فرمت ID: "msg_" + زمان فعلی + یک عدد تصادفی (مثال: msg_1704567890123_abc123)
3. این ID باید در تمام جاهایی که پیام ساخته می‌شه اضافه بشه:
   - در chatEngine.js وقتی پیام کاربر ساخته می‌شه
   - در chatEngine.js وقتی پیام مدل ساخته می‌شه
   - هنگام بارگذاری پیام‌ها از storage (اگه ID نداشتن، یکی بهشون بده)
4. در کدهای UI که از پیام استفاده می‌کنن، از message.id استفاده کن (مثلاً در دکمه Copy به جای اینکه کل آبجکت message رو پاس بدیم، فقط message.id رو پاس بدیم)
5. تمام پیام‌ها باید یک فیلد timestamp هم داشته باشن که زمان ایجاد پیام رو نشون بده.
توجه: این تغییر نباید عملکرد فعلی برنامه رو خراب کنه، فقط یک فیلد اضافی به پیام‌ها اضافه می‌کنیم.