# راهنمای کاربر گوگ (Goug)

## ۱. معرفی

### گوگ چیست؟
گوگ یک ابزار متن‌باز، رایگان و مدرن برای گفت‌وگو با مدل‌های پیشرفته هوش مصنوعی مانند Gemini و ChatGPT است. این برنامه کاملاً در مرورگر شما اجرا می‌شود و نیازی به نصب یا ثبت‌نام ندارد.

### چه مشکلی را حل می‌کند؟
بسیاری از کاربران دوست دارند از مدل‌های مختلف هوش مصنوعی استفاده کنند، اما هر کدام رابط کاربری و تاریخچه مجزای خود را دارند. گوگ با فراهم کردن یک **محیط یکپارچه**، به شما اجازه می‌دهد تا به راحتی بین ارائه‌دهندگان مختلف جابجا شوید و تمام گفتگوهای خود را در یک مکان مدیریت کنید.

### چرا از گوگ استفاده کنیم؟ (مزایا)
- **حریم خصوصی کامل**: تمام گفتگوها و کلیدهای API شما فقط در مرورگر خودتان ذخیره می‌شوند و به هیچ سروری ارسال نمی‌گردند.
- **بدون نیاز به سرور**: گوگ یک برنامه کاملاً سمت کاربر (Client-Side) است.
- **پشتیبانی از چند ارائه‌دهنده**: به راحتی بین مدل‌های Gemini, OpenAI و حتی مدل‌های محلی (مانند Ollama) جابجا شوید.
- **رابط کاربری سریع و زیبا**: طراحی مدرن و واکنش‌گرا که هم در دسکتاپ و هم در موبایل به خوبی کار می‌کند.
- **رایگان و متن‌باز**: شما می‌توانید کد منبع را مشاهده کرده و در توسعه آن مشارکت کنید.


## ۲. شروع سریع (۵ دقیقه)

1.  **دریافت کلید API**: از یکی از ارائه‌دهندگان هوش مصنوعی (مانند Google AI Studio) یک کلید API دریافت کنید.
2.  **اجرای گوگ**: برنامه را در مرورگر خود باز کنید. اگر ارائه‌دهنده پیش‌فرضی پیکربندی نشده باشد، پنجره تنظیمات به صورت خودکار نمایش داده می‌شود.
3.  **وارد کردن اطلاعات**: کلید API و نام مدل مورد نظر خود را در بخش مربوطه وارد کرده و روی «ذخیره تغییرات» کلیک کنید.
4.  **شروع گفتگو**: پنجره تنظیمات بسته می‌شود. حالا می‌توانید اولین پیام خود را تایپ کرده و ارسال کنید!


## ۳. تنظیمات اولیه

برای استفاده از گوگ، باید حداقل یک ارائه‌دهنده هوش مصنوعی را پیکربندی کنید. پنجره تنظیمات به شما امکان می‌دهد تا بین ارائه‌دهندگان معروفی مانند Gemini و OpenAI انتخاب کرده یا ارائه‌دهندگان سفارشی خود را اضافه کنید.

**توجه**: اگر برنامه با یک ارائه‌دهنده پیش‌فرض شروع به کار کند، شما نیازی به انجام این مراحل برای شروع گفتگو ندارید، اما همچنان می‌توانید کلید شخصی خود را برای جایگزینی مدل پیش‌فرض اضافه کنید.

### رابط کاربری جدید تنظیمات
پنجره تنظیمات از یک طراحی **آکاردئونی** استفاده می‌کند. هر ارائه‌دهنده در یک بخش جمع‌شونده قرار دارد که با کلیک روی عنوان آن باز و بسته می‌شود.

### انتخاب ارائه‌دهنده فعال
در کنار نام هر ارائه‌دهنده، یک **دکمه رادیویی (دایره‌ای)** وجود دارد. برای اینکه برنامه از یک ارائه‌دهنده خاص استفاده کند، باید دکمه رادیویی مربوط به آن را انتخاب کنید. تنها ارائه‌دهنده‌ای که به عنوان "فعال" انتخاب شده باشد، برای گفتگوها استفاده خواهد شد.

### پیکربندی Gemini یا OpenAI
1.  روی عنوان بخش Gemini یا ChatGPT کلیک کنید تا باز شود.
2.  **نام مدل** و **کلید API** خود را در فیلدهای مربوطه وارد کنید.
3.  دکمه رادیویی کنار عنوان را انتخاب کنید تا به عنوان ارائه‌دهنده فعال شما تنظیم شود.
4.  در انتها روی **«ذخیره تغییرات»** کلیک کنید.

### مدیریت ارائه‌دهندگان سفارشی (مثلاً Ollama)
بخش «مدل سفارشی» به شما اجازه می‌دهد تا چندین پیکربندی برای APIهای مختلف (مانند مدل‌های محلی Ollama یا سایر سرویس‌ها) اضافه و مدیریت کنید.

#### افزودن یک پیکربندی جدید
1.  در مقابل عنوان «مدل سفارشی»، روی دکمه **«+ افزودن»** کلیک کنید. یک بخش آکاردئون جدید با نام «پیکربندی جدید» اضافه می‌شود.
2.  روی این بخش جدید کلیک کنید تا باز شود.
3.  فیلدهای زیر را پر کنید:
    -   **نام پیکربندی**: یک نام منحصر به فرد برای شناسایی این تنظیمات (مثلاً `Ollama - Llama3`). این نام در عنوان آکاردئون نمایش داده می‌شود.
    -   **نام مدل**: نام مدلی که می‌خواهید استفاده کنید (مثلاً `llama3`).
    -   **کلید API**: کلید API در صورت نیاز (برای Ollama معمولاً هر مقداری کافی است).
    -   **آدرس نقطه پایانی API**: آدرس کامل API (مثلاً `http://localhost:11434/v1/chat/completions`).
4.  برای فعال کردن این پیکربندی، دکمه رادیویی کنار نام آن را انتخاب کنید.
5.  برای ذخیره، روی **«ذخیره تغییرات»** کلیک کنید.

#### حذف یک پیکربندی
-   موس را روی عنوان پیکربندی مورد نظر نگه دارید تا **آیکون سطل زباله (🗑️)** در سمت چپ نمایان شود.
-   روی آیکون کلیک کنید. یک پنجره تأیید نمایش داده می‌شود.
-   با تأیید، پیکربندی برای همیشه حذف خواهد شد.

### گزینه امنیتی "فقط برای این نشست"
در پایین پنجره تنظیمات، یک گزینه با عنوان **"فقط برای این نشست"** وجود دارد.
- **اگر این گزینه غیرفعال باشد (پیش‌فرض):** کلید API شما به صورت امن در پایگاه داده داخلی مرورگر (IndexedDB) ذخیره می‌شود تا در بازدیدهای بعدی نیز در دسترس باشد.
- **اگر این گزینه را فعال کنید:** کلید API شما **ذخیره نخواهد شد** و فقط در حافظه موقت (RAM) برای نشست فعلی نگهداری می‌شود. با بستن تب یا مرورگر، کلید به طور کامل پاک می‌شود و در بازدید بعدی باید دوباره آن را وارد کنید. این گزینه برای استفاده در دستگاه‌های عمومی یا مشترک توصیه می‌شود.


## ۴. کار با گپ‌ها (Chats)

گوگ تمام گفتگوهای شما را به صورت خودکار ذخیره می‌کند.

-   **ایجاد گپ جدید**: روی دکمه **«+ گپ جدید»** در پایین پنل کناری کلیک کنید.
-   **جابجایی بین گپ‌ها**: روی عنوان هر گپ در پنل کناری کلیک کنید تا آن را باز کنید.
-   **تغییر نام گپ**: موس را روی یک گپ نگه دارید، روی دکمه سه‌نقطه کلیک کرده و گزینه «تغییر نام» را انتخاب کنید.
-   **حذف گپ**: از همان منوی سه‌نقطه، گزینه «حذف» را انتخاب کنید. **توجه:** این عمل قابل بازگشت نیست.

### ۴.۱. مدیریت مدل در هر گپ
گوگ به شما اجازه می‌دهد تا برای هر گفتگوی خود، یک مدل هوش مصنوعی مجزا انتخاب کنید.

-   **شناسایی مدل**: در پنل کناری، کنار عنوان هر گپ یک **آیکون رنگی** نمایش داده می‌شود که نوع ارائه‌دهنده آن را مشخص می‌کند (آبی برای Gemini، سبز برای OpenAI و خاکستری برای مدل‌های سفارشی).
-   **انتخاب مدل**: در بالای صفحه، نام مدل فعلی گپ نمایش داده می‌شود. با کلیک روی این دکمه، یک منوی کشویی با تمام مدل‌های موجود باز می‌شود و شما می‌توانید مدل این گپ را در هر زمان تغییر دهید. در این منو، مدل فعال با یک **آیکون تیک (✓)** مشخص شده است.
-   **مدل پیش‌فرض**: هنگام ایجاد یک گپ جدید، برنامه به صورت خودکار از مدلی که شما در تنظیمات به عنوان "فعال" انتخاب کرده‌اید، استفاده می‌کند.
-   **هشدار مدل نامعتبر**: اگر شما یک پیکربندی را از تنظیمات حذف کنید، در کنار گپ‌هایی که از آن مدل استفاده می‌کردند، یک **آیکون هشدار (⚠️)** نمایش داده می‌شود. برای ادامه گفتگو، باید از منوی بالای صفحه یک مدل معتبر جدید انتخاب کنید، در غیر این صورت امکان ارسال پیام وجود نخواهد داشت.


## ۵. ارسال و دریافت پیام

-   **نوشتن پیام**: پیام خود را در کادر متنی در پایین صفحه تایپ کنید.
-   **ارسال پیام**: کلید `Enter` را فشار دهید یا روی دکمه ارسال کلیک کنید.
-   **ایجاد خط جدید**: برای رفتن به خط بعدی بدون ارسال پیام، `Shift+Enter` را فشار دهید.
-   **مشاهده پاسخ**: پاسخ مدل به صورت کلمه به کلمه (استریم) نمایش داده می‌شود. در حین دریافت پاسخ، دکمه ارسال غیرفعال می‌شود تا از ارسال پیام‌های همزمان جلوگیری شود.
-   **رونوشت (کپی) کردن پیام**: برای کپی کردن محتوای هر پیام، موس را روی حباب پیام نگه دارید تا آیکون رونوشت (`content_copy`) نمایان شود. با کلیک بر روی این دکمه، محتوای پیام در کلیپ‌بورد شما کپی می‌شود.


## ۶. ارسال تصویر

گوگ از ارسال تصویر در کنار متن پشتیبانی می‌کند (مدل‌های چندرسانه‌ای).

-   **پیوست کردن تصویر**: روی **آیکون گیره کاغذ (📎)** در کنار کادر ورودی کلیک کرده و تصویر مورد نظر خود را انتخاب کنید (فرمت‌های مجاز: PNG, JPG, GIF, WebP).
-   **مشاهده پیش‌نمایش**: پس از انتخاب، یک پیش‌نمایش کوچک از تصویر بالای کادر ورودی نمایش داده می‌شود. برای حذف تصویر قبل از ارسال، روی **آیکون ضربدر (×)** روی پیش‌نمایش کلیک کنید.
-   **ارسال**: شما می‌توانید تصویر را به تنهایی یا همراه با یک متن ارسال کنید.
-   **مشاهده در گپ**: برای مشاهده تصویر در ابعاد بزرگتر، روی آن در تاریخچه گفتگو کلیک کنید.


## ۷. ذخیره‌سازی و حریم خصوصی

**حریم خصوصی شما اولویت اصلی گوگ است.**

-   **ذخیره‌سازی محلی**: تمام داده‌ها، شامل تاریخچه گفتگوها و کلید API، در پایگاه داده داخلی مرورگر شما (IndexedDB) ذخیره می‌شوند.
-   **بدون جمع‌آوری داده**: گوگ هیچ‌گونه اطلاعاتی از کاربران جمع‌آوری نمی‌کند.


## ۸. میانبرهای کیبورد

| کلید           | عملکرد                                |
|-----------------|---------------------------------------|
| `Enter`         | ارسال پیام                            |
| `Shift` + `Enter` | ایجاد یک خط جدید در کادر ورودی        |
| `Esc`           | بستن نمای تمام‌صفحه تصویر             |


## ۹. رفع مشکلات رایج (Troubleshooting)

-   **مشکل: پیام خطای "تنظیمات API صحیح نیست" را دریافت می‌کنم.**
    -   **راه‌حل**: مطمئن شوید که کلید API خود را به درستی در تنظیمات وارد کرده‌اید. کلید را دوباره کپی و جای‌گذاری کنید. همچنین، نام مدل را بررسی کنید که صحیح باشد.

-   **مشکل: پیام ارسال می‌شود اما پاسخی دریافت نمی‌شود.**
    -   **راه‌حل ۱**: اتصال اینترنت خود را بررسی کنید.
    -   **راه‌حل ۲**: ممکن است کلید API شما منقضی شده یا اعتبار کافی نداشته باشد. به وب‌سایت ارائه‌دهنده (مثلاً گوگل) مراجعه کرده و وضعیت حساب خود را بررسی کنید.
    -   **راه‌حل ۳**: ممکن است API ارائه‌دهنده به طور موقت با مشکل مواجه باشد. کمی صبر کرده و دوباره تلاش کنید.

-   **مشکل: یک پیام خطای قرمز با متن "ذخیره‌سازی ناموفق بود" مشاهده می‌کنم.**
    -   **راه‌حل**: این خطا معمولاً موقتی است و زمانی رخ می‌دهد که پایگاه داده مرورگر به طور موقت در دسترس نباشد (مثلاً توسط یک تب دیگر قفل شده باشد). **نگران نباشید و صفحه را بازنشانی نکنید.** گوگ به صورت خودکار هر ۱۰ ثانیه یکبار دوباره برای ذخیره تلاش خواهد کرد. پس از ذخیره موفق، یک پیام سبز رنگ به شما نمایش داده خواهد شد.

-   **مشکل: تمام گپ‌های من پاک شده‌اند!**
    -   **راه‌حل**: گوگ داده‌ها را در حافظه مرورگر ذخیره می‌کند. اگر حافظه کش (Cache) مرورگر خود را پاک کرده باشید یا از حالت `Incognito/Private` استفاده کنید، ممکن است داده‌ها حذف شوند. متأسفانه، بازیابی آن‌ها ممکن نیست.

-   **مشکل: برنامه بارگذاری نمی‌شود و یک پیام خطا نمایش داده می‌شود.**
    -   **راه‌حل**: به جای یک صفحه سفید، گوگ تلاش می‌کند تا مشکل را به شما اطلاع دهد. به پیام نمایش داده شده توجه کنید:
        -   **"نسخه دیگری از برنامه باز است"**: این خطا یعنی شما گوگ را در یک تب دیگر باز کرده‌اید. لطفاً تمام تب‌های دیگر مربوط به گوگ را بسته و سپس روی دکمه "بارگیری مجدد" کلیک کنید.
        -   **"مرورگر شما پشتیبانی نمی‌شود"**: این یعنی مرورگر شما قدیمی است و از تکنولوژی‌های لازم برای ذخیره گفتگوها پشتیبانی نمی‌کند. لطفاً از یک مرورگر مدرن مانند گوگل کروم، فایرفاکس یا مایکروسافت اج استفاده کنید.
        -   **"مشکل در دسترسی به حافظه"**: این خطا معمولاً به دلیل تنظیمات حریم خصوصی سخت‌گیرانه مرورگر یا استفاده از حالت ناشناس (Incognito) رخ می‌دهد. لطفاً حالت ناشناس را غیرفعال کرده و تنظیمات مسدودکننده کوکی/ذخیره‌سازی مرورگر خود را بررسی کنید.


## ۱۰. پرسش‌‌های پرتکرار (FAQ)

-   **آیا گوگ رایگان است؟**
    بله، استفاده از خود نرم‌افزار گوگ کاملاً رایگان است. اما توجه داشته باشید که استفاده از API مدل‌های هوش مصنوعی (مانند Gemini یا OpenAI) ممکن است طبق تعرفه‌های آن شرکت‌ها هزینه داشته باشد.

-   **محدودیت‌های ارسال تصویر چیست؟**
    -   حجم فایل اصلی نباید بیشتر از ۱۰ مگابایت باشد.
    -   همچنین، حجم نهایی تصویر پس از فشرده‌سازی خودکار نباید از ۴ مگابایت بیشتر شود.

-   **آیا داده‌های من امن است؟**
    بله. همانطور که گفته شد، داده‌های شما فقط در مرورگر خودتان ذخیره می‌شوند. قابلیت "فقط برای این نشست" نیز یک لایه امنیتی اضافه برای استفاده در محیط‌های عمومی فراهم می‌کند.

-   **آیا می‌توانم در موبایل از گوگ استفاده کنم؟**
    بله، رابط کاربری گوگ کاملاً واکنش‌گرا (Responsive) است و به خوبی روی دستگاه‌های موبایل و تبلت کار می‌کند.

-   **چگونه می‌توانم از گپ‌ها نسخه پشتیبان تهیه کنم؟**
    در حال حاضر، گوگ گزینه داخلی برای پشتیبان‌گیری ندارد. افزودن قابلیت Export/Import یکی از برنامه‌های آینده برای توسعه این ابزار است.


## ۱۱. پشتیبانی و کمک

گوگ یک پروژه متن‌باز است. اگر با مشکلی مواجه شدید یا پیشنهادی برای بهبود آن دارید، لطفاً به [صفحه مشکلات (Issues) پروژه در گیت‌هاب](link-to-your-github-issues-page) مراجعه کرده و یک مورد جدید ثبت کنید.